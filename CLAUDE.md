# Claude Code Instructions

- AGENTS.md の内容に従うこと。
- GitHub Pages での確認を優先する。`main` への push で Pages デプロイが走る前提で作業する。
- ユーザーが明示的に求めた場合、コミットとプッシュを積極的に行ってよい。
- Pages 公開に影響する変更では、`npm run build` で静的ビルド成功を確認してからコミットする。
- テストは vitest で実行する: `npx vitest run`
